name: Simple CI

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  success-message:
    runs-on: ubuntu-latest
    steps:
      - name: Success Message
        run: echo "✅ CI Pipeline Success! Your push was detected."
